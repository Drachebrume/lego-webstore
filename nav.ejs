<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
      <ul class="navbar-nav">
        <li class="nav-item pr-2">
          <a class="navbar-brand" href="#"><img src="/images/logo_lego.jpg" width="30px"> WebStore</a>
        </li>
        <li class="nav-item pr-2">
          <a class="nav-link" href="/produits">Produits</a>
        </li>
        <li class="nav-item pr-2">
          <a class="nav-link" href="/evenements">Evènements</a>
        </li>
        <% if ( session.firstname ) { %>
          <li class="nav-item pr-2">
            <a class="nav-link" href="/panier">Panier</a>
          </li>
        <% }; %>     
      </ul>
      <ul class="nav navbar-nav ml-auto">
        <li class="nav-item">
            <% if ( session.firstname ) { %>
              <button type="button" class="btn btn-outline-secondary">
                <a href="/profil" style="color : grey"><i class="fas fa-user"> <%= session.firstname %></i></a>
              </button>
            <% }; %>
            <% if ( !(session.firstname) ) { %>
              <button type="button" class="btn btn-outline-secondary " data-toggle="modal" data-target="#exampleModal">
                <i class="fas fa-user"> Compte</i>
              </button>
            <% }; %>
          </button>
          <% if ( session.firstname ) { %>
            <form method="post" action="/deconnexion" style="display : inline">
              <button type="submit" class="btn btn-outline-secondary">
                <i class="fas fa-sign-out-alt"></i>
              </button>
            </form>
          <% }; %>
          </button>
        </li>
      </ul>
</nav>

<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Compte</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <form method="post" action="/connexion">
          <div class="form-group">
            <input type="text" placeholder="Identifiant" name="email2" class="form-control" id="ID">
          </div>
          <div class="form-group">
            <input type="password" placeholder="Mot de passe" class="form-control" name="passwd2" id="pwd">
          </div>
          <button type="submit" class="btn btn-block" style="background-color : grey; color : white;">Connexion</button>
        </form>
      </div>
      <div class="modal-footer">
        <a href="/inscription" class="btn btn-primary center" style="width : 100%">Inscription</a>
      </div>
    </div>
  </div>
</div>